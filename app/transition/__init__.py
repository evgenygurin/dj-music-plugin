"""Transition scoring domain — pure math, no I/O."""
# Public re-exports are temporarily disabled while the module is being
# refactored. Import directly from submodules (e.g. app.transition.scorer).
