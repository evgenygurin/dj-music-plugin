---
description: Sync a local playlist with Yandex Music (pull/push/diff).
agent: dj-music
---

Sync a playlist with Yandex Music:
1. Show diff with `dj_playlist_sync(playlist_id, direction=diff)`
2. On approval, run with the requested direction

User request: $ARGUMENTS
