{
  "mcpServers": {
    "dj-music": {
      "command": "bash",
      "args": [
        "-c",
        "cd /Users/laptop/dev/dj-music-plugin && set -a && source .env 2>/dev/null && set +a && exec uv run fastmcp run app/server.py"
      ]
    }
  }
}
