{
  "mcpServers": {
    "dj-music": {
      "command": "bash",
      "args": [
        "-c",
        "set -a; source .env 2>/dev/null; set +a; exec uv run fastmcp run app/server.py"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
